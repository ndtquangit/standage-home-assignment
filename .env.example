# ================================================
# Environment Configuration
# ================================================
# Copy this file to .env and update values
# NEVER commit .env to version control!
# ================================================

# Application
NODE_ENV=development
PORT=3000

# ================================================
# Database Configuration
# ================================================
# SECURITY: In production, use strong credentials
# and consider using a secrets manager
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=chatapp
DATABASE_PASSWORD=chatapp_password
DATABASE_NAME=chatapp

# ================================================
# JWT Configuration
# ================================================
# SECURITY WARNING: Change this in production!
# Generate a secure random string (min 32 chars):
#   openssl rand -base64 32
# The application will FAIL to start in production
# without a proper JWT_SECRET set.
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRATION=7d

# ================================================
# CORS Configuration
# ================================================
# Comma-separated list of allowed origins
# In production, only list your actual frontend domains
CORS_ORIGIN=http://localhost:5173,http://localhost:80
